name: 🚀🚀 Check project

on:
  push:
    branches: ['main']

jobs:
  web-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v3

      - name: Intall FE Node.js 25
        uses: actions/setup-node@v2
        with:
          node-version: '25'

      - name: 🔨 Run tests
        run: |
          npm install
          npm run test
